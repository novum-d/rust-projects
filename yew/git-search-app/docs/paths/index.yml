get:
  parameters:
     - in: query
       name: q
       description: keyword
       schema:
         type: string
  responses:
    '200':
      description: A list of repositories
      content:
        application/json:
          schema:
            repos:
              properties:
                items: 
                  $ref: schemas/responses/repos.yml
