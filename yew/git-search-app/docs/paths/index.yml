get:
  parameters:
     - in: query
       name: q
       schema:
         type: string
       description: keyword
  responses:
    '200':
      description: the user being returned
      content:
        application/json:
          schema:
            $ref: ../schemas/responses/repos.yml
